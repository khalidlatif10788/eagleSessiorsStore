# Preferred: Vite exposes only variables prefixed with VITE_
VITE_API_URL=http://127.0.0.1:8000

# Legacy (still read if you use vite.config dotenv + define)
# BASE_URL=http://127.0.0.1:8000

# Stripe publishable key (Checkout page)
# STRIPE_PUBLISHABLE_KEY=
